export const SubmitButton = ({value}) => {
    return <input type="submit" value={value} className="mt-4 w-full max-w-md p-4 bg-action hover:bg-primary rounded-md text-white font-semibold cursor-pointer transition-colors duration-200" />
}